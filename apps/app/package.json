{
  "name": "app",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "typecheck": "tsc --noEmit",
    "format": "prettier . --write",
    "lint": "eslint ."
  },
  "dependencies": {
    "astro": "^4.15.1",
    "@astrojs/check": "^0.9.3"
  },
  "devDependencies": {
    "config": "*"
  }
}
